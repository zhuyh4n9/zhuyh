#include<bits/std++.h>
#include"../zhuyh.hpp"

using namespace zhuyh;

void run()
{
  IAddress::ptr address = newAddressByHostAny("0.0.0.0");
  if(addr)
    LOG_ROOT_INFO() << *addr;
  else return;
}

int main()
{
  co run;
  return 0;
}
